/**
 * Bounty Compatibility Routes (Stub)
 * Bounty system compatibility endpoints
 */

import type { FastifyInstance } from 'fastify';

export async function bountyCompatibilityRoutes(_app: FastifyInstance): Promise<void> {
  // TODO: Implement bounty compatibility routes
}
