# Contract Change Ledger — Architecture Gap Remediation

## Shared-Contract Ownership / Order Rules
1. `prisma/schema.prisma`, `src/shared/*`, and cross-domain contract surfaces in `src/a2a/routes.ts` are the shared spine and must land before or alongside downstream wave changes that consume them.
2. Any intentional contract drift in routes, auth/authz, trust/quarantine, subscription entitlements, or model-tier checks must be logged in this ledger in the same change that introduces it.
3. Architecture-doc fidelity overrides current backward compatibility when repo evidence proves divergence, but every intentional break must include migration notes and the owning verification command set.
4. When entitlements change, rerun and log the owner suites for `src/subscription/*`, `src/model_tier/*`, `src/sandbox/*`, and `src/workerpool/*`.
5. When trust/quarantine or security semantics change, rerun and log `src/quarantine/*`, `src/a2a/routes.test.ts`, `src/marketplace/routes.test.ts`, and relevant `src/security/*` suites.

## Current Ledger Entries

| date | wave | surface | chapters | change_type | summary | migration_notes | verification_ref |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-04-15 | 0 / 0.5 setup | Shared contract spine | 3, 24, 29, 30, 38, 39 | planning-only | Initialized ledger and ownership rules; no route/schema/runtime contract changes have been introduced in this worker change yet. | Add concrete migration guidance here when shared contract or public API behavior changes land. | `verification-log-architecture-gap-remediation.md` (Wave 0 inventory, final global gates) |
| 2026-04-15 | 3 | Credit Marketplace parity slice | 21, 9, 20 | route + service contract | Realigned `/a2a/bid/place` to submit a bid against an existing bounty, added `/a2a/bid/accept`, exposed Chapter-21 aliases (`bid_amount`, `estimated_completion`, `proposal`, `reputation_escrow`, `reward_credits`), and enriched `/marketplace/stats` with price tiers, averages, top categories, and bounty counts. | Clients using the old self-bounty interpretation of `/a2a/bid/place` must now send `bounty_id`; `amount/estimatedTime/approach` remain accepted as compatibility aliases for `bid_amount/estimated_completion/proposal`. | `verification-log-architecture-gap-remediation.md` (Wave 3 Chapter 21 parity slice) |
| 2026-04-15 | 3 | Credit Marketplace schema alignment | 21, 9 | schema + service contract | Added persistent `milestones`, `winner_id`, and `reputation_escrow` fields to bounty models and updated refund/release paths so escrow is explicitly tracked instead of recomputed ad hoc. Also corrected dynamic pricing to use network asset GDI averages instead of node reputation averages. | Run `npm run db:generate` after pulling this change so Prisma Client reflects the new bounty schema. Existing callers may continue to read legacy bounty fields; the new aliases and stored fields are additive. | `verification-log-architecture-gap-remediation.md` (Wave 3 Chapter 21 parity slice) |
| 2026-04-15 | 3 | Credit Marketplace milestone workflow closure | 21 | schema + workflow contract | Promoted milestone workflow from JSON-only metadata to a first-class relational path with `BountyMilestone`, while retaining JSON as a compatibility mirror. Submit/review flows now sync relational milestone status, deliverable text, and partial payout state before final settlement. | Consumers can continue reading `Bounty.milestones`, but new code should prefer the relational milestone path when extending Chapter 21. | `verification-log-architecture-gap-remediation.md` (Final Global Gates — Post Chapter 21 follow-up) |
| 2026-04-15 | 3 | Premium Subscription downgrade scheduling | 39 | schema + public contract | Added scheduled downgrade fields to subscriptions, deferred downgrade plan changes to period-end, and surfaced scheduled-plan metadata plus `effective_immediately` semantics through the canonical subscription change/status responses. | Clients reading `/subscription` or `/subscription/change` can now distinguish immediate upgrades from period-end downgrades. Downstream code should check `scheduled_plan` before assuming the current plan is the next-cycle plan. | `verification-log-architecture-gap-remediation.md` (Wave 3 Chapter 39 subscription parity slice) |
| 2026-04-15 | 3 | Service Marketplace escrow settlement alignment | 20 | schema + route + service contract | Realigned Service Marketplace listing/purchase/detail contracts so listing creation reports the 5-credit listing fee, purchases lock buyer funds into persisted escrow-backed pending transactions, seller payout occurs on confirm instead of purchase, disputes now mark the underlying transaction as `disputed`, exclusive listings transition to `sold`, and canonical transaction detail now lives at `/marketplace/transactions/:id` (with `/transactions/detail/:id` preserved as a compatibility alias). Confirm/dispute route responses now surface top-level `transaction_id`, `amount`, `status`, and `escrow` fields alongside the canonical `data` payload. | Clients using the legacy asset-marketplace history route must move to `/marketplace/transactions/history/:nodeId`. Buyers/sellers can now rely on `status`, `platform_fee`, `seller_revenue`, and `escrow` fields in lifecycle/detail responses instead of inferring settlement state from side effects. | `verification-log-architecture-gap-remediation.md` (Wave 3 Chapter 20 service marketplace parity slice) |
| 2026-04-15 | 3 | `.gepx` binary wire-format closure | 19 | route + serializer + validation contract | Added the documented binary header/flags/payload-length encoding path, gzip-aware binary decode/validate helpers, and canonical `/gepx/export`, `/gepx/export/single`, `/gepx/validate`, and `/gepx/bundle/:id` aliases. This closes the main backend contract drift between the existing JSON/base64 helpers and the chapter’s specified `.gepx` transport format. | Existing `/bundles/*` CRUD remains valid; documented `.gepx` clients may now use the canonical export/validate/bundle alias routes and binary validation path without depending on bundle CRUD internals. | `verification-log-architecture-gap-remediation.md` (Wave 3 Chapter 19 gepx parity slice) |
| 2026-04-15 | 2 | Agent Directory & DM response-shape alignment | 24 | route + response contract | Kept directory and DM behavior inside A2A but aligned the response contracts with Chapter 24 by exposing top-level `agents`/`total`, `agent`, `dm_id`/`status`/`message`, `messages`, and `unread_count` fields while preserving the existing `success/data/meta` wrappers and recipient aliases. | Existing consumers can continue using `data/meta`; documented directory/DM clients may now rely on the chapter’s top-level response fields and the `to` alias on DM sends. | `verification-log-architecture-gap-remediation.md` (Wave 2 Chapter 24 directory and DM parity slice) |
| 2026-04-15 | 2 | Evolution Circle response-contract alignment | 22 | route + response contract | Aligned the bounded Chapter 22 backend surface by exposing documented create/list/detail aliases (`state`, `founder_id`, `members`, `config`, `current_round`, `updated_at`, and top-level `circle`/`circles` fields) while preserving the existing `success/data` wrappers and list-item compatibility keys. | Existing circle consumers can continue using `data.items` and `data` detail payloads; documented Chapter 22 clients may now rely on top-level create/list/detail fields without waiting for a full circle-round workflow rewrite. | `verification-log-architecture-gap-remediation.md` (Wave 2 Chapter 22 evolution circle parity slice) |
| 2026-04-15 | 2 | Hub Analytics & Monitoring dashboard/alerts alignment | 23 | route + service contract | Added documented monitoring aliases for `/dashboard/metrics` and `/alerts/stats`, and enriched `/alerts` with top-level `alerts` / `total` fields while preserving the legacy `success/data` response contract. Dashboard metrics now aggregate node, asset, quarantine, swarm, credits, and recent-alert counts from the current runtime and prisma state. | Existing `/monitoring/health`, `/monitoring/metrics`, and `/monitoring/alerts` consumers can keep reading `data`; documented dashboard/alert clients may now use the top-level dashboard metrics and alert list/stat fields without depending on internal wrappers. | `verification-log-architecture-gap-remediation.md` (Wave 2 Chapter 23 analytics and monitoring parity slice) |
| 2026-04-15 | 2 | Constitution & Ethics response-contract alignment | 25 | route + response contract | Aligned the bounded Chapter 25 backend surface by exposing top-level `rules` / `rule`, `constitution_version`, `profile`, `violations`, `amendment`, `amendments`, and amendment vote/ratify aliases while preserving the existing `success/data` wrappers and plural amendment compatibility paths. | Existing constitution clients can continue using the legacy `data` payloads; documented governance clients may now rely on the top-level constitution/amendment/ethics fields without waiting for deeper governance persistence changes. | `verification-log-architecture-gap-remediation.md` (Wave 2 Chapter 25 constitution and ethics parity slice) |
| 2026-04-15 | 2 | Periodic Sync response-contract alignment | 26 | route + response contract | Aligned the bounded Chapter 26 backend surface by exposing top-level `sync_id` / `status` / `completed_at` on trigger, top-level status fields plus derived `stats`, top-level `history` / `total`, and top-level `warnings` / `quarantine` on sync checks while preserving the legacy `success/data` wrappers. | Existing sync clients can continue reading `data`; documented sync clients may now rely on the top-level trigger/status/history/check fields without waiting for a full four-step sync-engine rewrite. | `verification-log-architecture-gap-remediation.md` (Wave 2 Chapter 26 periodic sync parity slice) |
| 2026-04-15 | 1 | Dispute & Arbitration response-contract alignment | 27 | route + response contract | Aligned the bounded Chapter 27 backend surface by exposing top-level `dispute` / `disputes`, `appeal` / `appeals`, `ruling`, `escalation`, `appeal_id`, `processed`, `dispute_id`, `arbitrators`, and `total` aliases across dispute create/list/detail, assignment, ruling, appeal, and escalation routes while preserving the existing `success/data` wrappers. | Existing dispute clients can continue using the legacy `data` payloads; documented dispute/arbitration clients may now rely on the top-level dispute/ruling/appeal fields without waiting for deeper message-thread or audit-chain persistence work. | `verification-log-architecture-gap-remediation.md` (Wave 1 Chapter 27 dispute and arbitration parity slice) |
| 2026-04-15 | 1 | Verifiable Trust response-contract alignment | 28 | route + response contract | Aligned the bounded Chapter 28 backend surface by exposing top-level reward/stake summary fields on claim, top-level `attestations` / `total`, and top-level `pending_stakes` / `total` while keeping the previously aligned stake/release/verify/level/stats compatibility fields and legacy `success/data` wrappers. | Existing trust clients can continue using `data`; documented trust clients may now rely on the top-level trust reward, attestation list, and pending-stake fields without waiting for deeper lifecycle/economic refinements. | `verification-log-architecture-gap-remediation.md` (Wave 1 Chapter 28 verifiable trust parity slice) |
| 2026-04-15 | 1 | Account & API key response-contract alignment | 29 | route + response contract | Aligned the bounded Chapter 29 backend surface by exposing top-level `token` / `user` on register/login, top-level API key creation fields, onboarding alias routes under `/account/onboarding/agent*`, and top-level `agents` / `total` on the user agent list while preserving the existing `success/data` wrappers and legacy onboarding routes. | Existing account clients can continue using the legacy `data` payloads; documented account/onboarding clients may now rely on the top-level account/API-key/onboarding fields without waiting for deeper agent-config registry changes. | `verification-log-architecture-gap-remediation.md` (Wave 1 Chapter 29 account and API key parity slice) |
| 2026-04-15 | 1 | Security control-plane response-contract alignment | 30 | route + response contract | Aligned the bounded Chapter 30 backend surface by exposing top-level `roles` / `total`, `result`, `assignment`, `events` / `total`, `event`, `report`, and `history` / `total` aliases across RBAC, rate-limit, security-event, and anomaly endpoints while preserving the existing `success/data` wrappers. | Existing security clients can continue using `data`; documented security-control-plane clients may now rely on the top-level RBAC/rate-limit/event/anomaly fields without waiting for a risky shared-auth refactor. | `verification-log-architecture-gap-remediation.md` (Wave 1 Chapter 30 security model parity slice) |
| 2026-04-15 | 0.5 / 1 | A2A hello/heartbeat response-contract alignment | 3 | route + protocol contract | Aligned the bounded Chapter 3 backend surface by exposing top-level hello and heartbeat handshake fields such as `your_node_id`, `node_secret`, `claim_code`, `claim_url`, `survival_status`, `recommended_tasks`, `network_manifest`, `available_tasks`, `overdue_tasks`, `peers`, and `server_time` while preserving the existing `success/data` wrapper. | Existing A2A clients can continue reading `data`; documented protocol clients may now rely on the top-level hello/heartbeat fields without waiting for deeper protocol-behavior expansion. | `verification-log-architecture-gap-remediation.md` (Wave 0.5/1 Chapter 3 A2A handshake parity slice) |
| 2026-04-15 | 1 | GDI analytics response-contract alignment | 4 | route + analytics contract | Aligned the bounded Chapter 4 backend surface by exposing public GDI trend/history/benchmark/improvement/prediction routes with top-level aliases (`points`, `history`, `benchmark`, `areas`, `current_gdi`, `predicted_gdi`, `confidence`) while preserving existing `success/data` wrappers and reputation endpoints. | Existing analytics clients can continue using `data`; documented GDI clients may now rely on the new top-level analytics fields without waiting for a full score-engine rewrite. | `verification-log-architecture-gap-remediation.md` (Wave 1 Chapter 4 GDI parity slice) |
| 2026-04-16 | 1 | Credits and reputation response-contract alignment | 5 | route + economy contract | Aligned the bounded Chapter 5 backend surface by exposing top-level credit balance/history/transfer fields, top-level credit price/economics fields, and top-level reputation score/history/leaderboard aliases while preserving the existing `success/data` wrappers. | Existing credits/reputation clients can continue using `data`; documented economy clients may now rely on the new top-level balance/history/leaderboard/economics fields without waiting for a deeper economy-engine rewrite. | `verification-log-architecture-gap-remediation.md` (Wave 1 Chapter 5 credits and reputation economy parity slice) |
| 2026-04-16 | 1 | Asset lifecycle response-contract alignment | 6 | route + asset contract | Aligned the bounded Chapter 6 backend surface by exposing top-level asset publish/fetch/revoke result fields, top-level search `assets` / `total`, and top-level GDI aliases on the dedicated asset routes while preserving the existing `success/data` wrappers. | Existing asset clients can continue using `data`; documented asset-lifecycle clients may now rely on the top-level publish/fetch/revoke/search/GDI fields without waiting for deeper lifecycle-engine changes. | `verification-log-architecture-gap-remediation.md` (Wave 1 Chapter 6 asset lifecycle parity slice) |
| 2026-04-16 | 1 | Worker Pool response-contract alignment | 8 | route + worker contract | Aligned the bounded Chapter 8 backend surface by exposing top-level worker registration/heartbeat/deregister fields, top-level `workers` / `worker` / `specialists` / `pools` / `matches` / `total` aliases, and top-level specialist rating / availability fields while preserving the existing `success/data` wrappers. | Existing workerpool clients can continue using `data`; documented Worker Pool clients may now rely on the top-level worker/specialist/match/stats fields without waiting for deeper scheduler or settlement changes. | `verification-log-architecture-gap-remediation.md` (Wave 1 Chapter 8 worker pool parity slice) |
| 2026-04-16 | 1 | Council governance response-contract alignment | 10 | route + governance contract | Aligned the bounded Chapter 10 backend surface by exposing top-level `proposal` / `proposals`, `vote` / `votes`, `result`, `config`, `dialogue`, `terms`, `term`, and council dispute-resolution aliases while preserving the existing `success/data` wrappers and compatibility routes. | Existing council clients can continue using `data`; documented governance clients may now rely on the top-level proposal/vote/config/history/term fields without waiting for a deeper governance-engine rewrite. | `verification-log-architecture-gap-remediation.md` (Wave 1 Chapter 10 council governance parity slice) |
| 2026-04-16 | 2 | Arena response-contract alignment | 14 | route + arena contract | Aligned the bounded Chapter 14 backend surface by exposing top-level `season`, `battle`, `battles`, `leaderboard`, `total_participants`, `seasons`, and matchmaking aliases while preserving the existing `success/data` wrappers. | Existing arena clients can continue using `data`; documented arena clients may now rely on the top-level season/matchmaking/battle/leaderboard fields without waiting for a deeper Elo or matchmaking rewrite. | `verification-log-architecture-gap-remediation.md` (Wave 2 Chapter 14 arena parity slice) |
| 2026-04-16 | 2 | Drift Bottle response-contract alignment | 18 | route + async-help contract | Aligned the bounded Chapter 18 backend surface by exposing top-level bottle/rescue status fields on throw/discover/reply/discard and by adding the documented list/stats/inbox/detail/pick/rescue/reject aliases while preserving the existing `success/data` wrappers. | Existing drift-bottle clients can continue using `data`; documented async-help clients may now rely on the top-level bottle/rescue fields and alias endpoints without waiting for a deeper drift-engine rewrite. | `verification-log-architecture-gap-remediation.md` (Wave 2 Chapter 18 drift bottle parity slice) |
| 2026-04-16 | 2 | Skill Store response-contract alignment | 16 | route + skill-store contract | Aligned the bounded Chapter 16 backend surface by exposing top-level categories/featured/stats/my/rating aliases plus top-level `skill`, `skills`, `total`, `deleted`, `result`, and `rating` fields across list/detail/download and mutation routes while preserving the existing `success/data` wrappers. | Existing skill-store clients can continue using `data`; documented Skill Store clients may now rely on the top-level listing/detail/stats/mutation fields without waiting for deeper moderation or distillation engine changes. | `verification-log-architecture-gap-remediation.md` (Wave 2 Chapter 16 skill store parity slice) |
| 2026-04-16 | 2 | Anti-hallucination response-contract alignment | 17 | route + safety contract | Aligned the bounded Chapter 17 backend surface by exposing top-level `validation`, `detection`, `check`, `patterns`, `anchors`, `nodes`, `edges`, `chain`, `report`, and related totals/summary aliases across anti-hallucination routes while preserving the existing `success/data` wrappers and compatibility endpoints. | Existing anti-hallucination clients can continue using `data`; documented safety clients may now rely on the top-level check/detect/confidence/pattern/graph fields without waiting for deeper engine changes. | `verification-log-architecture-gap-remediation.md` (Wave 2 Chapter 17 anti-hallucination parity slice) |
| 2026-04-16 | 2 | Memory Graph response-contract alignment | 15 | route + graph contract | Aligned the bounded Chapter 15 backend surface by exposing top-level `node`, `edge`, `root` / `lineage`, `chain`, `results`, `patterns`, decay summaries, export/import counts, and related totals across the spec-facing Memory Graph routes while preserving the existing `success/data` wrappers. | Existing memory-graph clients can continue using `data`; documented memory-graph clients may now rely on the top-level graph/lineage/recall/export/import fields without waiting for a deeper graph-engine rewrite. | `verification-log-architecture-gap-remediation.md` (Wave 2 Chapter 15 memory graph parity slice) |

| 2026-04-16 | 2 | Evolution Sandbox entitlement and membership alignment | 13, 39 | route + authz contract | Sandbox routes now require an active Premium/Ultra subscription before non-public sandbox access, invite acceptance preserves `participant`/`observer` roles, and spec-facing detail/member responses map the internal creator-owner record back to the chapter’s public participant role. | Free-plan callers now receive a 403 when accessing authenticated sandbox surfaces; existing Premium/Ultra clients keep the same endpoints but should rely on the public `participant`/`observer` roles instead of the internal owner marker in API responses. | `verification-log-architecture-gap-remediation.md` (Wave 2 Chapters 13 + 38 sandbox/model-tier parity slice) |
| 2026-04-16 | 2 | Model Tier Gate claim/evaluation guidance alignment | 38 | route + gate contract | Declared-model evaluation now composes with stored tier state, claim allow-lists only bypass minimum tiers when the model actually matches, and model-tier routes now expose top-level catalog/agent/evaluation/claim fields plus structured `TIER_INSUFFICIENT` guidance payloads. | Existing clients can keep using `data`; documented Tier Gate clients may now rely on top-level `tiers`, `tier`, `agent`, `evaluation`, `claim`, `assessment`, `upgrade_paths`, and `models` fields plus the explicit insufficiency payload without depending on wrapper internals. | `verification-log-architecture-gap-remediation.md` (Wave 2 Chapters 13 + 38 sandbox/model-tier parity slice) |

## Watchlist for Future Entries
- A2A directory / DM semantics that move or rename endpoints
- Shared auth or API-key semantics in `src/shared/auth.ts` / `src/account/*`
- Quarantine and verifiable trust gating behavior
- Subscription plan semantics, grace periods, entitlement propagation, and model-tier gates
- Marketplace/service exposure changes, especially when mirrored through `src/a2a/routes.ts`
