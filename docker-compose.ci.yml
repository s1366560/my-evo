# CI-specific docker-compose override
# Removes host-port publishing to avoid port conflicts in Drone runner
# The deploy stage uses host.docker.internal to reach containers

services:
  db:
    ports: []

  redis:
    ports: []

  backend:
    ports: []

  frontend:
    ports: []
